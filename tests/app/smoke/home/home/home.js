import { Given, When, Then } from "cypress-cucumber-preprocessor/steps";
 
Given('Given I visit the homepage', () => {
	cy.Pending();
});

Then('Then I verified title', () => {
	cy.Pending();
});

When('When I verified the dropdown menu is visible', () => {
	cy.Pending();
});
