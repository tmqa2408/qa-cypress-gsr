{
  "name": "mobmo-cypress",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^14.0.1"
  },
  "scripts": {
    "clean-reports": "rm -f reports/*.xml",
    "run-tests": "npx cypress run"
  },
  "dependencies": {
    "esbuild": "^0.24.2"
  }
}
